import 'package:flutter/material.dart';

class AppColors {
  // Colores principales
  static const Color primary = Color(0xFFDC2626); // Rojo para botones e íconos
  static const Color secondary = Color(
    0xFF4A90E2,
  ); // Azul de apoyo si necesitas
  static const Color accent = Color(0xFFFFC107); // Amarillo opcional (alertas)

  // Fondo y superficies
  static const Color background = Color(0xFFF5F5F5); // Gris muy claro
  static const Color surface = Colors.white; // Tarjetas y contenedores

  // Texto
  static const Color textPrimary = Color(0xFF212121); // Negro fuerte
  static const Color textSecondary = Color(0xFF757575); // Gris medio

  // Estados
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFD32F2F);
  static const Color warning = Color(0xFFFFA726);

  // Extras
  static const Color border = Color(0xFFE0E0E0); // Bordes suaves
  static const Color shadow = Colors.black12; // Sombra ligera
}
